# 《枪械工艺 / GUNSMITH》游戏设计提示词

> 给任何实现者（AI 或人）的完整设计提示词。照着这份文档做，就是一个可以玩的、类《魔法工艺》的枪械构筑 roguelike。

## 0. 一句话定位

一款在 HTML5 Canvas 里运行的俯视角 roguelike 射击游戏：玩家在随机生成的科幻设施中探索房间，用拾取到的**枪械部件**拼装独一无二的武器，清怪、开箱、换装、打 Boss。灵感来自《魔法工艺》（Magicraft），但把"法杖+法术宝石"换成"枪械+部件构筑"。

## 1. 硬性约束

- **不用任何图片/精灵图素材**：所有视觉均由 Canvas 2D 代码绘制（几何图形、线条、粒子、发光）。
- **场景简单但有科技感、细腻**：不追求写实场景，靠发光描边、网格、扫描线、粒子、色彩分层营造"高精尖实验室/太空站"质感。
- **生物是简单的不同颜色圆圈**：敌人一律用圆形 + 颜色 + 内核图案区分行为，禁止画复杂造型。
- **打击感必须细致、分层**：不是简单"屏幕震动 + 闪屏"，而是多系统协同的反馈链（见第 4 节，这是本作的重点）。
- **先有提示词再开工**：本文件即提示词，实现前先按本文件对齐。

## 2. 核心玩法循环

```
生成设施(随机房间网格) → 探索房间 → 遭遇战(圆形敌人) → 清怪开门
→ 战利品(宝箱/商店/精英掉落: 枪械部件) → 拼装/换枪 → 下一房间
→ 每 4~6 个房间一个精英/Boss 节点 → 通关或死亡重开
```

- 一局时长目标：10~15 分钟。
- 死亡即重开（roguelike），**不做局外永久成长**（v1）。
- 可同时携带 **2 把枪**，数字键/滚轮切换；每把枪 **3 个部件槽**。

## 3. 枪械构筑系统（对标魔法工艺的宝石插槽）

每把枪由 3 个部件拼装，部件决定武器行为：

| 槽位 | 决定内容 | 示例 |
|---|---|---|
| **枪身**（基础） | 射速、基础伤害、弹容量、换弹速度 | 手枪身(均衡)、冲锋枪身(高射速)、霰弹枪身(多弹片)、狙击枪身(高伤慢速)、机枪身(大弹鼓) |
| **弹药**（弹道） | 弹道形态与命中特效 | 穿甲弹(穿透)、分裂弹(命中分裂)、追踪弹(微追踪)、爆炸弹(范围)、电磁链(电链传导)、扇形散弹、锯齿弹(持续伤害) |
| **配件**（被动） | 属性与异常状态 | 暴击模块、射速芯片、吸血泵、燃烧弹头、冰冻核心、后坐力稳定器、弹药回收、处决(低血斩杀) |

### 部件规则

- 部件有 **稀有度**：白 < 绿 < 蓝 < 紫 < 橙，影响词条数量与数值。
- 每个部件带 1~3 条**随机词条**（如：射速+18%、穿透+1、暴击率+12%、换弹-25%、子弹分裂为 3、命中减速 20%）。
- 相同弹药槽的部件可叠加效果（如两个"爆炸弹"提升爆炸半径与伤害），同类词条上限有封顶，避免数值爆炸。
- 掉落来源：宝箱（普通）、商店（花钱/献祭血量）、精英敌人（必掉紫以上）、Boss（橙）。
- 开局赠送一把默认手枪，保证随时能打。

## 4. 打击感系统（重点，分层设计）

打击感 = 以下 **6 层** 反馈在同一帧/几十毫秒内协同触发，按触发点组合，**禁止只做屏幕震动和闪屏**。

### 4.1 射击层（开枪瞬间）
- **后坐力**：准星/武器模型向上后跳再回落（带衰减弹簧），连续射击时准星轻微扩散，停止后收敛。
- **枪口火光**：3~5 帧的星形/多边形火光 + 发光渐变，随射速缩放。
- **弹壳抛飞**：每发一颗小矩形弹壳，带旋转与抛物线，落地弹跳一次渐隐。
- **开火烟**：枪口一小团扩散渐隐的烟圈。
- **曳光弹**：子弹飞行时带拖尾光带，弹速越快拖尾越长；狙击弹拖尾更亮更粗。
- **枪声**：WebAudio 合成，不同枪身有不同音色（手枪脆、霰弹闷、机枪哒哒），带轻微随机音高抖动。

### 4.2 命中层（子弹打中瞬间）
- **方向性火花**：命中点朝子弹来向的反方向溅射 4~8 粒小粒子（颜色与目标阵营匹配）。
- **受击白闪**：敌人本体闪白 2~3 帧（不是全屏闪）。
- **击退**：敌人被推开一小段距离，重型敌人抵抗强（击退弱），Boss 基本免疫。
- **伤害飘字**：命中位置冒出小数字（伤害值），暴击时数字变大变金色并带星爆。
- **命中音**：短促的"啪/噗"，与枪声区分，保证"开火"和"打中"是两个听感。

### 4.3 击杀层（敌人死亡瞬间）
- **碎裂**：敌人圆形炸成 6~10 个同色小色块，带自旋向外飞散并渐隐。
- **Hit-stop**：击杀瞬间全局停顿约 50~70ms（画面静止、音效暂停），让这一击"有重量"。
- **慢动作**：精英/Boss 死亡时 0.2~0.4 秒的 0.3x 时间膨胀，配合碎裂粒子。
- **连杀反馈**：0.5 秒内连杀会累积"连击数"，飘字放大、周围泛微光，连击越高音调越高。

### 4.4 环境层
- **弹痕**：子弹打在墙壁/地面上留下渐隐的发光痕迹。
- **尸体残留**：碎裂粒子散落在地面持续 1~2 秒再消失（不瞬移消失）。
- **击杀音**：与命中音不同，带一点"闷响/回响"。

### 4.5 屏幕层（克制使用）
- **轻微震屏**：仅用于 暴击、爆炸命中、精英/Boss 死亡、Boss 大招，位移 2~6px 且带衰减回弹。
- **边缘辉光**：玩家低血量时屏幕四周呼吸性红光，不是全屏闪。
- **禁用**：无脑全屏白闪；所有屏幕效果都要有"触发理由"。

### 4.6 通用原则
- 所有粒子走**对象池**，上限约 1500 个，不卡顿。
- 反馈**可叠加但不过载**：同一帧最多 1 个 hit-stop + 1 个慢动作。
- 默认帧率 60，物理固定步长 + 渲染插值，保证反馈时序稳定。

## 5. 视觉设计（纯代码绘制）

### 5.1 总体
- 主色：深蓝黑背景 `#0a0e16`，霓虹青 `#35e0ff` + 品红 `#ff3d81` + 琥珀 `#ffb347` 做点缀。
- 地面：透视网格线 + 房间矩形发光边框；墙壁用半透明深色 + 发光线描边。
- 全局可加一层极淡的扫描线/噪点（canvas 自绘，不引入图片），强化科技感。
- 文本：等宽字体（monospace），HUD 用代码感排版（生命条用分段格子）。

### 5.2 玩家
- 主体为几何感三角形/六边形机甲（代码绘制），枪口方向跟随鼠标。
- 移动时尾部喷小粒子；受伤时本体红闪 + 一小圈冲击波。
- 换枪时枪口造型/颜色随部件改变（给玩家"这把枪不一样"的直观反馈）。

### 5.3 敌人（统一：不同颜色圆圈 + 内核图案）

| 颜色 | 行为 | 描述 |
|---|---|---|
| 红 | 近战冲锋 | 圆 + 实心内核，靠近后蓄力冲撞，冲撞前有预备闪白 |
| 橙 | 远程点射 | 圆 + 中心圆点，站在射程内间断开火（弹速慢可躲） |
| 绿 | 分裂 | 圆 + 内核小圆，死亡分裂成 2 个小圆（属性递减） |
| 蓝 | 高速穿梭 | 小圆 + 速度光环，直线冲刺折返，命中率低但骚扰强 |
| 紫 | 精英 | 大圆 + 外圈护盾环，需先打掉护盾（护盾带蓄力高亮） |
| Boss | 机械核心 | 多层同心圆 + 旋转外环 + 发射环，2~3 个阶段（弹幕→召唤→狂暴） |

- 每种敌人受击白闪、死亡碎裂的颜色与其本体色一致。
- 敌人体型 = 血量感：同色敌人分小/中/大三档，越大越慢越肉。

### 5.4 UI/HUD
- 左下：生命格子条 + 护盾条（若有）。
- 右下：当前武器图标（用部件拼出的示意图）+ 弹药数 + 换弹提示。
- 顶部：当前房间数、金币、连击数。
- 交互：宝箱开箱有"闪光 + 部件卡弹出"动画；商店为 3 个部件选择格。

## 6. 房间生成与关卡

- 设施 = 网格地图，用简单随机算法保证所有房间连通（BFS 验证）。
- 房间类型：战斗房（多数）、宝箱房、商店房、精英房、Boss 房。
- 战斗房清怪后：房门亮起 + 上方飘出"门已开启"提示，地面门缝发光。
- 每层 4~6 个房间，打完 Boss 进下一层（数值+15%，颜色主题微调）。
- 地图上显示"下一房间方向"的微弱光点指引，避免迷路。

## 7. 技术架构（单文件 HTML）

- **单文件**：`index.html`（内联 CSS + JS），双击即玩，无外部依赖、无网络请求。
- Canvas 2D + `requestAnimationFrame`；固定时间步（1/120s 逻辑更新 + 插值渲染）。
- 模块分层（文件内按区块组织，每块职责单一）：

```
输入层(键盘/鼠标) → 玩家控制 → 武器系统(部件解析→弹道生成)
→ 子弹系统(对象池) → 敌人AI(状态机: 巡逻/追踪/攻击/硬直)
→ 碰撞检测(圆形判定) → 反馈系统(粒子/飘字/弹痕/后坐力)
→ 屏幕系统(震屏/hit-stop/慢动作/辉光) → 音频系统(WebAudio 合成)
→ 房间/关卡系统 → UI/HUD → 主循环
```

- **部件数据**：集中在 `PARTS` 数据表（纯数据，不含逻辑），新部件 = 新增一条数据 + 对应行为函数注册。
- **对象池**：粒子、子弹、弹壳、飘字全部复用，避免 GC 卡顿。
- **音效**：WebAudio 振荡器/噪声合成（枪声、命中、拾取、门开），无音频文件。
- 容错：`window.onerror` 显示错误遮罩，方便调试。

## 8. v1 范围（本次实现）

**做：**
- 完整可玩循环：开始界面 → 设施探索 → 战斗/宝箱/商店/精英/Boss → 死亡结算/通关结算。
- 2 把枪位、每把 3 槽；部件库 ≥ 15 种（枪身 5 + 弹药 6 + 配件 6，含随机词条）。
- 5 种普通敌人 + 1 个 Boss（多阶段）。
- 完整打击感 6 层系统 + WebAudio 音效。
- 地图生成 + 房间类型 + 数值平衡（保证 10~15 分钟一局）。

**不做（YAGNI）：**
- 局外成长/存档/成就、多角色、联机、手柄支持、图片素材。
- 皮肤系统、剧情文本、多语言（界面中文即可）。

## 9. 成功标准

1. 双击 `index.html` 即玩，无报错、不掉帧（普通笔记本 60fps）。
2. 一局内能体验到：3 种以上部件组合出的"手感完全不同的枪"。
3. 打击感可被明确感知：开火、命中、击杀三个时刻的反馈明显不同且克制、不糊眼。
4. 敌人仅用彩色圆圈，但玩家能凭颜色+内核立即读懂行为。
5. 不出现全屏无脑闪屏；所有屏幕特效都有触发原因。

---

### 待确认的决策点

- **A. 游戏模式**：推荐"房间探索式 roguelike"（对标魔法工艺本体）；备选"波次生存简化版"（更快做出但少了探索构筑感）。
- **B. 代码形态**：推荐单 HTML 文件（方便分发试玩）；备选 `index.html + game.js` 分离（好维护）。
- **C. 操作方式**：推荐 WASD 移动 + 鼠标瞄准/射击 + 滚轮切枪；备选纯键盘（自动瞄准）。
- **D. 主题命名**：默认科幻实验室/太空站 + 中文名《枪械工艺》。

### 已确认决策（2026-08-06）

- **A**：房间探索式 roguelike。
- **B**：单 HTML 文件。
- **C**：WASD 移动 + 鼠标瞄准/射击 + 滚轮/数字键切枪。
- **D**：中文名《枪械工艺》，科幻实验室/太空站主题。
